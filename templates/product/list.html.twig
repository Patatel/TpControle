{% extends 'base.html.twig' %}

{% block body %}

    <h1>Product List</h1>
    <ul>

    {% for product in products %}
        <li>{{ product.nom }}&nbsp;{{ product.prix }} : [ <a href="{{
        path('product_edit', {'id': product.id}) }}">Edit</a> - <a href="{{ path('product_delete',
        {'id': product.id}) }}">Delete</a> - <a href="{{ path('feedback') }}">Feedback</a> ]</li>
    {% endfor %}

    </ul>
    <a href="{{ path('productform') }}">Add a product</a>
    
{% endblock %}